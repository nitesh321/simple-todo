
<%= form_with(model: todo_list, local: true) do |form| %>
  <% if todo_list.errors.any? %>
    <div>
      <p class"help"><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</p>

      <ul>
      <% todo_list.errors.full_messages.each do |message| %>
        <li class="help"><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, {:class => 'label'} %>
    <%= form.text_field :title, {:class => 'input'} %>
  </div>

  <div class="field" >
    <%= form.label :description, {:class => 'label'} %>
    <%= form.text_area :description, {:class => 'textarea'} %>
  </div>

  <div class="actions">
    <%= form.submit class:'button is-link is-small'%>
  </div>
<% end %>
